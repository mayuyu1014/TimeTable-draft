package com.ulan.timetable.utils;

public class SampleUnit {
    //a method which accepts 2 numbers, adds and returns the sum
    public int addNumbers(int num1, int num2){
        int sum = num1 + num2;
        return sum;
    }
}
